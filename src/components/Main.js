import React from 'react'
import reactCSS from 'reactcss'

export const Main = () => {
  const styles = reactCSS({
    'default': {

    },
  })

  return (
    <div>Main</div>
  )
}

export default Main
