import React from 'react'
import reactCSS from 'reactcss'

export const Header = () => {
  const styles = reactCSS({
    'default': {

    },
  })

  return (
    <div>Header</div>
  )
}

export default Header
